{%- macro photo(photo) %}
{%- set photoUrl = photo.value if photo.value else photo %}

<figure>
  <a href="{{ photoUrl }}">
    <img class="u-photo shadow rounded" src="{{ photoUrl }}"{%- if photo.alt %} alt="{{ photo.alt }}"{%- endif %}>
  </a>
</figure>

{%- endmacro %}
