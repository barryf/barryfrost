{%- extends 'layout.njk' %}
{%- import 'macros/icon.njk' as icon %}

{%- block head %}
<link rel="me" href="mailto:auth@barryfrost.com">
<link rel="micropub" href="{{ process.env.MICROPUB_URL }}">
<link rel="webmention" href="{{ urls.webmention }}">
<link rel="microsub" href="https://aperture.p3k.io/microsub/28">
<link rel="authorization_endpoint" href="https://indieauth.com/auth">
<link rel="token_endpoint" href="https://tokens.indieauth.com/token">
<title>Barry Frost</title>
{%- endblock %}

{%- block content %}
<h1 class="h-card flex mb-8 text-gray-800 dark:text-gray-200">
  <img src="{{ urls.favicon }}" class="u-photo w-6 h-6 md:w-7 md:h-7 rounded-full mr-1.5" alt="Barry Frost">
  <span class="p-name uppercase text-lg md:text-xl leading-6 md:leading-7">Barry Frost</span>
</h1>

<p class="mb-8 md:text-lg">
  The page or post. This is my website and here's some text about me. If you want to read this
  then go ahead and keep reading right until the end. It may even last for a
  few lines of text.
</p>

{%- set postTypes = ['Article', 'Note', 'Photo', 'Bookmark', 'Checkin', 'Reply', 'Like', 'Repost', 'RSVP'] %}
{%- set klass = 'hover:text-yellow-600 inline-block border-gray-300 dark:border-gray-700 hover:border-yellow-600 border-solid border-2 rounded-lg px-2 md:px-3 pt-2 pb-1.5 leading-none mb-2 md:mb-2.5 mr-1 shadow-sm' %}
<div class="mb-8 text-lg md:text-xl text-gray-600 dark:text-gray-400">
  <a href="/all" class="{{ klass }}">
    <span class="whitespace-nowrap">
      {{ icon.icon('all') }}
      All
    </span>
  </a>
  {%- for type in postTypes %}
    {%- set plural = type + 's' if type != 'Reply' else 'Replies' %}
    <a href="/{{ plural | lower }}" class="{{ klass }}">
      <span class="whitespace-nowrap">
        {{ icon.icon(type | lower) }}
        {{ plural }}
      </span>
    </a>
  {%- endfor %}
</div>

{%- endblock %}
